package com.suyako.lazyQoL.common.function;

/*import com.suyako.lazyQoL.core.event.CustomEventHandler;
import com.suyako.lazyQoL.core.mcef.example.BrowserManager;
import net.minecraftforge.common.MinecraftForge;
import net.minecraftforge.fml.common.eventhandler.SubscribeEvent;*/

public class WebBrowser {
    /*public WebBrowser() {
        MinecraftForge.EVENT_BUS.register(this);
        CustomEventHandler.EVENT_BUS.register(this);
    }
    @SubscribeEvent
    public void onDisable(CustomEventHandler.FunctionEnableEvent event){
        if(event.function.getName().equals("WebBrowser")){
            BrowserManager.INSTANCE.openBrowser();
            event.setCanceled(true);
        }
    }
    @SubscribeEvent
    public void onSwitch(CustomEventHandler.FunctionSwitchEvent event){
        if(event.function.getName().equals("WebBrowser") && (event.status)){
            BrowserManager.INSTANCE.openBrowser();
            event.setCanceled(true);
        }
    }*/
}
